# Recursive
Translation that thinks twice. A local-first desktop application for translating large documents with recursive semantic verification — translate, validate, correct, repeat until meaning is preserved.
